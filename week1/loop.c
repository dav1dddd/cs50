#include <stdio.h>
#include <stdbool.h>

int main(void) {
    // Using the boolean expression "true" will ensure that this loop runs forever.
    // basically saying "on the condition that this is true, run whatever is in the curly braces forever."
    while (true) {
        printf("beans\n");
    }
}
